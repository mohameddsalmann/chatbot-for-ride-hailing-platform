# SmartLine AI Chatbot V3 - Nginx Configuration
# Add this to your existing nginx config or include it

# Chatbot upstream
upstream chatbot_backend {
    server 127.0.0.1:3001;
    keepalive 32;
}

# Add these location blocks to your existing server block:

# Chatbot API endpoints
location /chatbot/ {
    rewrite ^/chatbot/(.*) /$1 break;
    proxy_pass http://chatbot_backend;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_cache_bypass $http_upgrade;
    proxy_read_timeout 60s;
    proxy_send_timeout 60s;
}

# Alternative: Direct path (if you want /chat instead of /chatbot/chat)
# location /ai-chat {
#     proxy_pass http://chatbot_backend/chat;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
# }

# Chatbot health check
location /chatbot-health {
    proxy_pass http://chatbot_backend/health;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
}
